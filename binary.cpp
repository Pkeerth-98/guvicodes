#include<iostream.h>
#include<conio.h>
int main()
{
char a[100];
  int c;
cout<<"enter the string";
cin>>a[100];
if(a[0]==0)
  ++c;
 if(a[0]==1)
    ++c;
 if(c!=0)
cout<<"it is binary";
else
cout<<"not binary";
getch();
return 0;
}
